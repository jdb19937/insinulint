int main() {
    if (
        x && y &&
        z == w &&
        foo == bar
    ) {
        exit(0);
    }

    if (z) {
        1;
    }
}

int bigfunc(
    int x,
    int y,
    int z
) {
    return z;
}

